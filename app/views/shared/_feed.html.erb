<% if @feed_items.any? %>
<ol class="microposts">
<%= render @feed_items %>
</ol>
<%= will_paginate @feed_items,
params: { controller: :stactic_pages, action: :home } %>   <%#atranh loi khi tao micropost moi%>
<% end %>